# Closed Task Archive

**Status**: Archived closed-task ledger (non-authoritative)
**Quarter**: 2026-Q3

Do not read `archive/closed_tasks/` during normal implementation flow unless a user explicitly asks for historical context or an archive-aware CLI flag is used.

---
### TASK-415: Adopt Context7 docs lookup package set
**Priority**: P2
**Estimate**: S

Adopt a short agent-facing Context7 docs lookup convention for third-party
libraries used by Horadus without making Context7 a runtime dependency or
source-of-truth replacement.

**Files**: `ops/skills/horadus-cli/SKILL.md`, `docs/adr/009-agent-facing-horadus-cli.md`

**Gate Upkeep Files**: `docs/AGENT_RUNBOOK.md`, `docs/ARCHITECTURE.md`,
`docs/ENVIRONMENT.md`, `docs/RELEASING.md`, `uv.lock`,
`docker/postgres/Dockerfile`

**Acceptance Criteria**:
- [x] Document the core/high-value Context7 library IDs and the on-demand package IDs from INTAKE-0072 in an agent-facing repo surface.
- [x] State that Horadus local runtime truth, task context packs, docs, and tests remain authoritative for workflow, task lifecycle, probability math, migrations, and domain contracts.
- [x] Keep Context7 lookup guidance limited to current third-party library/API docs and avoid routine lookups for low-value type/package tooling unless the task needs them.

**Completion Notes**:
- Targeted tests for Context7 guidance: N/A, docs-only agent guidance update.
- Gate upkeep: refreshed stale required docs-freshness markers, updated
  dependency-audit-blocked lockfile packages, and fixed the pgvector Postgres
  image toolchain selection after CI surfaced a Timescale multi-arch clang
  mismatch.
- Integration proof: `docker build --pull --no-cache --platform linux/amd64 -t geoint-postgres:ci-repro -f docker/postgres/Dockerfile docker/postgres`;
  `uv run --no-sync horadus tasks local-gate --full`.
- Follow-up intake captured for gate blockers: `INTAKE-0073`, `INTAKE-0074`.

---
### TASK-417: Clear full local-gate blockers surfaced by TASK-416
**Priority**: P0
**Estimate**: XS

Clear the repository-wide blockers surfaced by the `TASK-416` full local gate:
the stale deployment guide verification marker and the current dependency
audit findings for `click` and `lxml-html-clean`.

**Assessment-Ref**:
- TASK-416 local-gate blocker 2026-07-21
- INTAKE-0074 re-audit 2026-07-21

**Files**: `docs/DEPLOYMENT.md`, `pyproject.toml`, `uv.lock`

**Acceptance Criteria**:
- [x] Review docs/DEPLOYMENT.md against current deployment configuration and update stale content as needed.
- [x] Update the Last Verified marker only after verification.
- [x] Resolve `click` to at least 8.3.3 and `lxml-html-clean` to at least 0.4.5 without vulnerability allowlist exceptions.
- [x] The docs-freshness check and canonical full local gate pass.

**Implementation Notes**:
- Verified the documented compose services, images, profiles, networks, ingress headers, health endpoints, Make targets, environment variables, and referenced runbooks against current repo truth.
- Targeted tests: N/A — dependency metadata and docs/ledger-only changes with no executable code change.
- Integration proof: N/A — no integration-covered runtime path changed.
- Canonical proof: all 17 full local-gate steps passed, including dependency audit and Docker-backed integration.

---
### TASK-416: Update dependency audit CVE blockers
**Priority**: P0
**Estimate**: XS

Refresh the frozen dependency set so the canonical security audit no longer
blocks normal task delivery.
Current reproduced findings: click 8.3.1 PYSEC-2026-2132 and lxml-html-clean
0.4.4 PYSEC-2026-2614.

**Assessment-Ref**:
- INTAKE-0074 re-audit 2026-07-21

**Files**: `pyproject.toml`, `uv.lock`, `scripts/run_dependency_audit.sh`

**Acceptance Criteria**:
- [x] The lockfile resolves click to at least 8.3.3 and lxml-html-clean to at least 0.4.5 without unrelated direct-dependency upgrades.
- [x] ./scripts/run_dependency_audit.sh passes without adding vulnerability allowlist exceptions.
- [x] make secret-scan security dependency-audit and the canonical full local gate pass.

**Implementation Notes**:
- Resolved `click` to 8.4.2 and `lxml-html-clean` to 0.4.5; the related `lxml` patch release moved to 6.1.1.
- Integration proof: N/A — this task changes dependency metadata and the lockfile only, without an integration-covered runtime path.
- Docs update: N/A — the dependency security floor does not change an operator-facing or runtime contract.
- Targeted proof: `make secret-scan security dependency-audit`, the context-pack workflow suite (856 passed), and `make agent-check` passed.
- Delivery note: the dependency metadata landed on `main` through blocker `TASK-417` / PR #387 to break the circular dependency between the stale-docs and dependency-audit full-gate failures; this branch verifies and closes the original promoted task.
- Canonical proof: all 17 full local-gate steps passed on the post-`TASK-417` baseline.

---
### TASK-419: Update pyasn1 for dependency audit advisories
**Priority**: P0
**Estimate**: XS

TASK-418 full local gate is blocked at dependency-audit because frozen pyasn1
0.6.3 has actionable PYSEC-2026-3455, PYSEC-2026-3456, and PYSEC-2026-3457
advisories, all fixed in 0.6.4. Update the dependency on a dedicated task
branch so TASK-418 stays scope-pure.

**Files**: `pyproject.toml`, `uv.lock`

**Acceptance Criteria**:
- [x] pyasn1 is constrained and locked at 0.6.4 or newer so all three disclosed advisories clear.
- [x] The dependency audit and canonical full local gate pass without an allowlist waiver.

**Completion Notes**:
- Integration proof: N/A — this task changes only the dependency constraint and lockfile.
- Documentation update: N/A — no runtime or operator-facing contract changes.
- Security proof: `./scripts/run_dependency_audit.sh` passed with no actionable vulnerabilities.
- Canonical proof: all 17 steps of `horadus tasks local-gate --full` passed, including dependency audit, lockfile parity, Docker integration, and package build.

---

### TASK-418: Fix unrecorded LLM spend when post-call budget check denies
**Priority**: P0
**Estimate**: M

Persist actual LLM usage even when the completed call crosses the daily budget,
so subsequent pre-call checks fail closed instead of retrying paid calls
indefinitely.

**Files**: `src/processing/cost_tracker.py`, `tests/unit/processing/test_cost_tracker.py`, `tests/unit/processing`

**Acceptance Criteria**:
- [x] A completed LLM call that crosses the daily cost limit is recorded in api_usage before BudgetExceededError is raised.
- [x] The next pre-call budget check observes the recorded overage and blocks without another provider call.
- [x] Existing pre-call budget and per-tier call-limit behavior remains covered by focused unit and pipeline regression tests.

**Completion Notes**:
- Targeted proof: 133 focused cost-tracker, LLM-policy, pipeline, and classifier tests passed.
- Integration proof: all 20 Docker integration tests passed, including concurrent call- and cost-limit overage accounting.
- Documentation update: `docs/ARCHITECTURE.md` now states that completed provider calls are recorded before the defer signal is raised.

---

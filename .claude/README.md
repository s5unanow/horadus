# Claude Code Policy Notes

`settings.local.json` is a local override and should remain untracked.

Recommended setup:
1. Copy `.claude/settings.example.json` to `.claude/settings.local.json`.
2. Adjust local command allowlist as needed for your workstation.
3. Keep the local file private; only the example policy is versioned.
